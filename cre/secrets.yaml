# Maps a secret id (referenced in the workflow via runtime.getSecret) to the
# env var in cre/.env that holds its value. Deployed secrets are managed with
# `cre secrets create|update|list|delete`.
secretsNames:
  NAV_API_KEY:
    - NAV_API_KEY
