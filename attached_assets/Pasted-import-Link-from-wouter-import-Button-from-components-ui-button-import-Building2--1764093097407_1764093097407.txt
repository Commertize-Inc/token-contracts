import { Link } from "wouter";
import { Button } from "@/components/ui/button";
import { Building2, DollarSign, Coins, Shield, Warehouse, Home, Building, Layers, Sun, Wind } from "lucide-react";
import { motion } from "framer-motion";

const propertyTypes = [
  { id: "multifamily", name: "Multifamily", icon: Building2 },
  { id: "office", name: "Office", icon: Building },
  { id: "industrial", name: "Industrial", icon: Warehouse },
  { id: "mixed-use", name: "Mixed Use", icon: Layers },
  { id: "condominium", name: "Condominium", icon: Home },
  { id: "solar-farms", name: "Solar Farms", icon: Sun },
  { id: "wind-farms", name: "Wind Farms", icon: Wind },
];

const paymentMethods = [
  { name: "USD", icon: DollarSign, description: "Traditional payments", currencies: ["USD"] },
  { name: "Stablecoins", icon: Coins, description: "Digital currency", currencies: ["USDC", "USDT"] },
];

const SubmitProperty = () => {
  return (
    <section className="py-20 bg-white">
      <div className="container max-w-6xl mx-auto">
        <div className="text-center mb-16">
          <h2 className="text-4xl font-logo font-light text-foreground mb-3">
            List Your Property on <span className="text-primary">Commertize</span>
          </h2>
          <div className="w-20 h-1 bg-gradient-to-r from-primary to-yellow-600 rounded-full mx-auto mb-6"></div>
          <p className="text-foreground/80 mb-12 leading-relaxed max-w-4xl mx-auto text-lg">
            Connect your commercial property to a worldwide network of qualified investors. Our platform makes it simple to tokenize your CRE and open it to fractional investment.
          </p>
        </div>
            
        <div className="mb-12">
          <h3 className="text-3xl font-light font-logo mb-12 text-foreground text-center">
            Your Property, Our Global Marketplace
          </h3>
          
          {/* Rotating Network */}
          <div className="mb-16">
            <div className="relative h-[600px] w-full flex items-center justify-center overflow-hidden bg-white rounded-2xl border border-gray-200">
              <motion.div
                className="relative"
                animate={{ rotate: 360 }}
                transition={{ duration: 35, repeat: Infinity, ease: "linear" }}
              >
                {propertyTypes.map((type, index) => {
                  const angle = (index / propertyTypes.length) * 360;
                  const radius = 200;
                  const x = Math.cos(angle * Math.PI / 180) * radius;
                  const y = Math.sin(angle * Math.PI / 180) * radius;
                  
                  return (
                    <motion.div
                      key={type.id}
                      className="absolute"
                      style={{ left: x - 60, top: y - 18 }}
                      animate={{ rotate: -360 }}
                      transition={{ duration: 35, repeat: Infinity, ease: "linear" }}
                    >
                      <div className="flex items-center space-x-2 bg-gradient-to-br from-primary/5 to-primary/10 border-2 border-primary/40 rounded-lg px-3 py-2">
                        <div className="w-7 h-7 rounded-full bg-primary/20 border border-primary/50 flex items-center justify-center">
                          <type.icon className="w-4 h-4 text-primary" />
                        </div>
                        <div className="text-xs font-semibold text-gray-800">{type.name}</div>
                      </div>
                    </motion.div>
                  );
                })}
              </motion.div>
              
              {/* Central Logo */}
              <div className="absolute">
                <img src="/assets/logo.png" alt="Logo" className="w-32 h-32 object-contain" />
              </div>
            </div>
          </div>

          {/* Payment Methods */}
          <div className="space-y-6 text-center">
            <h4 className="text-xl font-light font-logo text-foreground flex items-center justify-center gap-2">
              <Shield className="w-6 h-6 text-primary" />
              Accepted Payment Methods
            </h4>
            <div className="grid grid-cols-1 sm:grid-cols-2 gap-8 max-w-2xl mx-auto">
              {paymentMethods.map((method) => (
                <div key={method.name} className="group">
                  <div className="p-8 rounded-2xl bg-white border border-gray-100 hover:border-primary/30 hover:shadow-xl transition-all duration-300">
                    <div className="flex flex-col items-center text-center space-y-4">
                      <div className="w-16 h-16 rounded-2xl bg-gradient-to-br from-primary/10 to-primary/20 flex items-center justify-center">
                        <method.icon className="w-8 h-8 text-primary" />
                      </div>
                      <div>
                        <div className="text-xl font-bold text-gray-900 mb-1">{method.name}</div>
                        <div className="text-sm text-gray-500">{method.description}</div>
                        {method.currencies.length > 1 && (
                          <div className="flex items-center justify-center gap-2 mt-2">
                            {method.currencies.map((currency) => (
                              <span key={currency} className="px-3 py-1 bg-primary/10 text-primary text-xs font-semibold rounded-full">
                                {currency}
                              </span>
                            ))}
                          </div>
                        )}
                      </div>
                    </div>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </div>

        <div className="text-center">
          <p className="text-foreground/80 mb-8 leading-relaxed text-lg max-w-3xl mx-auto">
            Take advantage of our global reach—submit your property today.
          </p>
          <Link href="/submit">
            <Button size="lg" className="bg-primary hover:bg-primary/90 shadow-lg text-lg px-8 py-4">
              Submit Your Property
            </Button>
          </Link>
        </div>
      </div>
    </section>
  );
};

export default SubmitProperty;